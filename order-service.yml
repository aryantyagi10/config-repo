server:
  port: 8081  # <--- CHANGED PORT!

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_service
  kafka:
    bootstrap-servers: pkc-xrnwx.asia-south2.gcp.confluent.cloud:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    properties:
      security.protocol: SASL_SSL
      sasl.mechanism: PLAIN
      sasl.jaas.config: org.apache.kafka.common.security.plain.PlainLoginModule required username=CSHAAOIG4GN6NDTM password=cflt7Akde4BkIqnVU4K6bX3sz/cMIjNjpuPyIocL7Qy7NhUrfDeWBcf44TQurf/g;


resilience4j:
  circuitbreaker:
    instances:
      inventory: # <--- We name our circuit breaker "inventory"
        registerHealthIndicator: true  #Exposes circuit breaker status via Actuator. We can see: OPEN, CLOSED, HALF_OPEN
        slidingWindowType: COUNT_BASED 
        slidingWindowSize: 5           # Check the last 5 calls
        failureRateThreshold: 50       # If 50% fail, stop calling Product Service
        waitDurationInOpenState: 5s    # Wait 5 seconds before trying again


